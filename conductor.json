{
  "scripts": {
    "setup": "cp $CONDUCTOR_ROOT_PATH/.env .env && cp $CONDUCTOR_ROOT_PATH/.claude/settings.local.json .claude/settings.local.json && bun install",
    "archive": "rm -rf node_modules"
  }
}
