{
  "scripts": {
    "setup": "bun install && cp $CONDUCTOR_ROOT_PATH/.env .env && cp $CONDUCTOR_ROOT_PATH/.claude/settings.local.json .claude/settings.local.json",
    "archive": "rm -rf node_modules"
  }
}
